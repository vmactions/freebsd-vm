

VM_NAME=FreeBSD

VM_RUNS_ON=macos-12

VM_PREPARE="pkg install -y curl"

VM_RUN=@.github/data/run.txt

VM_SET_RELEASE=13.0

VM_SHELL_COMMENTS="The default shell in FreeBSD is `csh`, if you want to use `sh` to execute the `run` script, please set `usesh` to `true`."

