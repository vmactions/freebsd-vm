

VM_NAME=FreeBSD

VM_RUNS_ON=ubuntu-latest

VM_PREPARE="pkg install -y curl"

VM_RUN=@.github/data/run.txt

VM_SET_RELEASE=13.2

VM_SHELL_COMMENTS="The default shell in FreeBSD(before 14.0) is `csh`, if you want to use `sh` to execute the `run` script, please set `usesh` to `true`."

TEST_RELEASES=@conf/test.releases


